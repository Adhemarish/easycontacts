<%= simple_form_for note, url: note_path(note), wrapper: :inline_form,
  html: { class: "form-inline" }  do |f| %>
<%= f.input :content, label: false%>
<%= f.button :submit, 'Sauvegarder', class: 'btn btn-primary'%>
<% end %>


<!-- , , method: :patch, -->
