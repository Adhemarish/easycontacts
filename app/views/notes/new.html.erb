<h2><%= @contact.first_name %> <%= @contact.last_name %></h2>

<div class="container">
  <div class="row ">
    <%= render 'notes/contactnote', contact: @contact, note: Note.new %>
  </div>
</div>

<%= link_to 'Retour', contact_path(@contact) %>

